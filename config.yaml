app:
  host: "0.0.0.0"
  port: 8000
  reload: true

auth:
  allowed_email_domain: "@tp.com.vn"

processing:
  max_length: 32
  lock_timeout: 60
  inference_threads: 10
  batch_max_threads: 32
  batch_max_urls: 50
